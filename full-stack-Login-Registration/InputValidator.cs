namespace full_stack_Login_Registration
{
    public class InputValidator
    {
        public bool ConfirmRegisterInputs(User user)
        {
            // Logic here for checking if register inputs are valid.
            return false; // <- temp to avoid the compiler giving error.
        }
    }
}
